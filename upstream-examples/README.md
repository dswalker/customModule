# Upstream examples

These images came with the Ex Libris `customModule` repository. They are kept
here for reference but intentionally live outside `src/assets`, so they are not
included in the CSU Central Package.
